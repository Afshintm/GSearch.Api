using System;
using System.Net.Http;
using Xunit;

namespace GSearch.Services.UnitTests
{
    public class GoogleSearchTests
    {
        [Fact]
        public void Test1()
        {
            //var fakeHandler = new FakeMessageHandler();
            //var client = new HttpClient(fakeHandler);
            //var googleSearch = new NewGoogleSearch(client);

            //googleSearch.
            //googleSearch.Search
            Assert.True(true);
        }
    }
}
